# ──────────────────────────────────────────────────────────────────────────
# FitAI Backend — Python Dependencies
# Pure-Python wheels — NO C++ Build Tools required.
#
# NOTE: We use the IBM Watsonx.ai REST API directly via `requests` instead of
# the ibm-watsonx-ai SDK, which pulls in ibm-cos-sdk (requires C++ on Windows).
# ──────────────────────────────────────────────────────────────────────────

flask==3.1.1
flask-cors==5.0.1
python-dotenv==1.1.1
requests==2.32.3
gunicorn==23.0.0
